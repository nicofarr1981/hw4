<h1>Places</h1>

<% if @current_user %>
  <p>
    <a href="/places/new" class="btn btn-secondary">New place</a>
  </p>
<% end %>

<ul>
  <% for place in @places %>
    <li class="m1"><a href="/places/<%= place["id"] %>" class="btn btn-info"><%= place["name"] %></a></li>
  <% end %>
</ul>